import React from "react";
import UserAddress from "./UserAddress";

const ProfileRightSection = ({ userInfo }) => {
  return <UserAddress userInfo={userInfo} />;
};

export default ProfileRightSection;
